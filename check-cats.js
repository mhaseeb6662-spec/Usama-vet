const { PrismaClient } = require('@prisma/client'); const prisma = new PrismaClient(); async function main() { console.log(await prisma.category.findMany({ include: { _count: { select: { products: true } } } })); console.log(await prisma.product.findMany({ take: 3, select: { id: true, name: true, categoryId: true, isActive: true } })); } main().finally(() => prisma.$disconnect());
